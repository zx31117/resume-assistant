# 架构与产品决策记录

> 文档角色：跨版本决策及其演进  
> 说明：只保留已有依据；没有证据的理由不补写

## 状态定义

- Accepted：当前仍有效；
- Partially Superseded：部分被后续调整；
- Superseded：已被后续决策替代；
- Rejected：评估后明确不采用；
- Planned：尚未通过实现验收。

## D-001 经历是资产，简历是输出

- 版本：V1.0
- 状态：Accepted
- 背景：产品需要支持不同 JD、模板以及后续面试和画像能力。
- 决策：Experience 独立持久化，简历按任务实时生成。
- 依据：如果把逻辑绑定到简历文件，新增模板、投递和面试能力会反复解析和迁移简历。
- 影响：SQL Experience 成为长期事实源。

## D-002 隔离 AI 框架

- 版本：V1.0
- 状态：Accepted
- 演进：实际边界比初始方案更严格。
- 决策：业务模块不直接依赖 LangChain，通过适配服务调用 AI。
- 初始方案：llm_service 和 rag_service 两个模块允许导入 LangChain。
- 实际结果：因 Embedding 特殊协议，rag_service 改用原生 HTTP，当前仅 llm_service 导入 LangChain。
- 影响：业务、API 和数据库不感知具体 AI 框架。

## D-003 豆包 Embedding 改用多模态模型

- 版本：V1.0
- 状态：Accepted
- 原计划：doubao-embedding-text-240715。
- 实际选择：doubao-embedding-vision-251215，使用纯文本输入。
- 依据：原计划模型不存在或不可用，旧文本模型已下线；当时平台可用的是多模态 Embedding。
- 影响：模型通过环境变量配置，向量维度变为实际模型维度。

## D-004 Embedding 使用原生 HTTP

- 版本：V1.0
- 状态：Accepted
- 原计划：LangChain OpenAIEmbeddings。
- 决策：rag_service 使用 urllib 调用 embeddings/multimodal。
- 依据：
  - LangChain 会将文本转成 token ID，而豆包接口要求原始字符串；
  - 豆包使用专属 endpoint；
  - 请求是对象数组；
  - 响应 data 是对象而不是 OpenAI 风格数组。
- 影响：rag_service 不再导入 LangChain。

## D-005 Chroma 不可用时提供 numpy 回退

- 版本：V1.0
- 状态：Partially Superseded
- 后续决策：D-012 恢复 Chroma 为主后端，但保留本决策定义的 numpy 故障回退。
- 背景：Windows 环境的 Chroma Rust 扩展/DLL 加载失败。
- 决策：保持统一存储接口，失败时使用 numpy 余弦检索和 JSON 持久化。
- 依据：V1 单用户、少量经历场景需要先跑通。
- 影响：上层不感知后端变化，但出现双后端和迁移需求。

## D-006 程序确定结构，AI 理解内容

- 版本：V1.1
- 状态：Accepted
- 背景：V1 全文经历提取耗时 201.5 秒，LLM 同时承担清洗、章节识别、定位和结构化。
- 决策：程序预处理和章节切分，分段并发调用 AI，再合并去重。
- 结果：经历提取实测降至 39.8 秒。

## D-007 RAG 使用多因素评分

- 版本：V1.1
- 状态：Accepted
- 后续优化：权重调优留 V2。
- 背景：语义相似不等于岗位匹配。
- 决策：Final = Semantic 0.5 + Skill 0.3 + Role 0.2。
- 影响：返回分项评分和匹配原因。
- 已知问题：技能分最高仅约 5%，区分度不足。

## D-008 结构化输出失败后的默认兜底

- 版本：V1.1
- 状态：Partially Superseded
- 决策：Structured Output → JSON + Pydantic → 降温重试 → 默认空模型。
- 当时依据：优先保证固定 Pipeline 不因格式错误中断。
- 后续问题：关键阶段可能把真实失败伪装成空成功。
- V1.3 演进：JD 和定制内容等关键阶段已经改为 strict failure；默认空模型只可保留在非关键旧路径或调试功能。

## D-009 放弃任意 Word 模板解析

- 版本：V1.2
- 状态：Rejected
- 被否决方案：用户上传 Word → 解析文本框 → 识别章节 → 按坐标写入。
- 实测依据：
  - 多个章节共享相同坐标；
  - 家族识别按内容会漏识别，按坐标会误合并；
  - 标题和内容框容易混淆；
  - 坐标去重会删除其他章节；
  - 复杂度接近实现 Word 渲染器子集。
- 结论：该能力不属于 V1.2。

## D-010 标准模板与数据展示分离

- 版本：V1.2
- 状态：Accepted
- 决策：
  - 使用系统内置 DOCX + TemplateSpec JSON；
  - 引入 ResumeDocument；
  - 按命名 style 定位；
  - 克隆原型段并只替换文本；
  - Profile 不从模板取默认数据；
  - 内容选择前移至 ResumeBuilder。
- 依据：标准化输入可以得到确定性结构，并避免模板样例污染用户事实。

## D-011 模板视觉参照改用用户 PDF

- 版本：V1.2
- 状态：Accepted
- 适用范围：V1.2 系统参考模板。
- 背景：初版模板参照 Word 文本框布局，与用户实际 PDF 的页边距、bullet、照片框和对齐差异较大。
- 决策：使用 pdfplumber 提取用户 PDF 的布局参数，重建系统标准模板。
- 结果：布局参数核对 46/46，用户数据填充 21/21。
- 注意：这些是 V1.2 模板验收，不等于严格跨软件像素级渲染。

## D-012 恢复 Chroma 原生后端并保留迁移能力

- 版本：V1.2.1
- 状态：Accepted
- 背景：环境已经可以正常加载 Chroma；默认 onnx embedding 路径仍可能触发 DLL 问题。
- 决策：
  - 使用 PersistentClient；
  - 所有写入显式传 embeddings；
  - 所有查询显式传 query_embeddings；
  - 保留 numpy 回退；
  - 增加 numpy → Chroma 幂等迁移和状态快照。
- 影响：V1.0 的“当前运行在 numpy”状态被更新，但历史原因保留。

## D-013 V1.2.1 使用真实验证环境锁定依赖

- 版本：V1.2.1
- 状态：Accepted
- 决策：依赖按已经通过 E2E 的实际环境精确锁定。
- 依据：该版本目标是可重现，不承担依赖升级和最低兼容版本研究。
- 影响：升级策略推迟到后续版本。

## D-014 PII 不依赖 gitignore 保护

- 版本：V1.2.1
- 状态：Accepted
- 决策：源码和测试数据直接替换为虚构 mock，不把真实 PII 移入被忽略的本地文件。
- 依据：gitignore 只能防止未来提交，不能清除历史对象。
- 影响：代码、运行产物和 tracked 文件执行分层扫描。

## D-015 V1.3 改为核心链路收口

- 版本：V1.3
- 状态：Accepted
- 背景：
  - V1.2.1 曾把下一版临时规划为 Git 历史和 GitHub 发布准备；
  - 架构审核发现 JD 驱动 DOCX 主链路仍未闭环；
  - 用户重新明确 V1/V2/V3 边界。
- 决策：V1.3 优先完成核心 JD → DOCX 流程；排版体验留 V2，服务器化留 V3。
- 影响：历史 GitHub 发布计划被后续决策替代，但保留在 V1.2.1 历史文档中。

## D-016 Builder 是唯一内容选择入口

- 版本：V1.3
- 状态：Accepted
- 背景：V1.2.1 的直接 ResumeDocument 路径绕过 Builder，Renderer 还会按模板截断。
- 决策：核心路径由 Builder 决定条目集合；Renderer 只渲染，max_items 仅告警。
- 依据：同一 ResumeDocument 更换模板时，业务内容集合不应变化。

## D-017 V1 不以一页纸为验收条件

- 版本：V1.3
- 状态：Accepted
- 决策：V1 只验证内容和大体结构；超页告警但不阻塞。
- 依据：用户明确将排版细节归入 V2。
- 影响：LayoutOptimizer 不得为了满足页数删除事实内容。

## D-018 V1 不生成个人总结或自我评价

- 版本：V1.3
- 状态：Accepted
- 背景：个人总结信息密度低，标准模板没有该章节；V1.3 第三轮修正前的实现仍会生成后跳过，形成无价值的调用和文档噪声。
- 决策：V1 不生成、不渲染个人总结或自我评价，也不把缺少该章节视为错误。
- 依据：核心价值来自可验证的职业经历；个人总结仅可能在履历单薄时用于补充表达。
- 影响：相关生成与模板能力留到 V2/V3 按实际场景评估。

## D-019 身份信息和求职意向使用显式来源

- 版本：V1.3
- 状态：Accepted
- 决策：
  - 姓名、电话、邮箱、所在地只使用用户显式输入；缺失就留空，禁止从数据库、AI、模板或职业经历推测/回填；
  - V1.3 的求职意向只来自当前 JD 的 `JDAnalysis.position`；
  - 未来版本可允许用户明确指定求职意向，但职业经历库永远不保存该字段。
- 依据：身份信息宁缺毋错；求职意向随每次投递的目标岗位变化，不是稳定职业资产。
- 影响：取消 `DB User` 和 AI 对最终身份信息的 fallback；Profile 来源应明确区分 request 与 JD。

## D-020 运行数据与源码仓库物理解耦

- 版本：V1.4
- 状态：Planned
- 背景：当前项目准备公开到 GitHub。若用户输入、生成输出、数据库、向量索引、日志和缓存仍与源码目录混放，每次发布都需要重新人工筛选和整理，既增加维护成本，也容易遗漏敏感数据。
- 决策：
  - 所有可变运行数据统一存放在一个可配置的 runtime data root 下，默认位置必须在 Git checkout 之外；
  - 源码仓库只保留代码、不可变产品资产、公开文档、虚构测试数据和配置样例；
  - 测试使用临时目录和虚构 fixture，不读写开发者的真实 runtime 数据；
  - 因旧 Git 历史曾包含 PII，V1.4 首次公开时只执行一次干净历史迁移；之后以新仓库为开发基线，正常提交和推送，不再逐版本制作人工发布快照；
  - 后续提交仍保留自动 Secret、PII 和运行产物检查，作为安全兜底，而不是人工整理流程。
- 依据：用物理边界消除长期混放风险，比每次发布人工筛选文件更稳定，也能降低人和执行 Agent 的重复工作与上下文负担。
- 影响：V1.4 需要统一路径配置，并对既有本地数据采用可验证、可回滚的迁移或重建；数据迁移、持久化和安全边界必须由可读取源码的高性能 Agent 独立复核。不得借此改变 API、事实模型或 V1.3 业务行为。
